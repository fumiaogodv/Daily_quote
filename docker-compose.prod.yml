version: "3.9"

services:
  quotes:
    image: quotes:prod
    container_name: quotes_prod

    ports:
      - "5000:5000"

    volumes:
      # ❗只挂载数据，不挂载代码
      - ./data:/app/data

    environment:
      - FLASK_ENV=production

    restart: unless-stopped
